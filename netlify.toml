# Rewrite all jamboree paths to the single-build app.
# The app resolves its config from the URL at runtime.
[[redirects]]
  from = "/jamboree_*"
  to = "/app.html"
  status = 200

[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "24"
