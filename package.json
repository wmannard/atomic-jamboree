{
  "name": "qa-atomic-commerce",
  "description": "A sample project for testing and validating the commerce use case in @coveo/atomic",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "preview": "npx vite preview",
    "build": "node ./scripts/copyResources.mjs && npx vite build && node ./scripts/generateDistIndex.mjs",
    "start": "npm run build && npx vite preview"
  },
  "dependencies": {
    "@coveo/atomic": "^3.60.0",
    "@coveo/headless": "^3.53.0"
  },
  "devDependencies": {
    "fs-extra": "^11.3.2",
    "vite": "7.2.4"
  }
}