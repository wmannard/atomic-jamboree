{
  "name": "qa-atomic-commerce",
  "description": "A sample project for testing and validating the commerce use case in @coveo/atomic",
  "type": "module",
  "scripts": {
    "preview": "npx vite preview",
    "build": "node ./scripts/copyResources.mjs && bash ./build-all.sh && node ./scripts/generateDistIndex.mjs",
    "build-on-windows": "node ./scripts/copyResources.mjs && call build-all.bat && node ./scripts/generateDistIndex.mjs",
    "start": "npm run build && npx vite preview",
    "start-on-windows": "npm run build-on-windows && npx vite preview"
  },
  "dependencies": {
    "@coveo/atomic": "^3.39.0",
    "@coveo/headless": "^3.35.1"
  },
  "devDependencies": {
    "fs-extra": "^11.3.2",
    "vite": "7.2.4"
  }
}
