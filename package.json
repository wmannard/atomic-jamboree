{
  "name": "qa-atomic-commerce",
  "description": "A sample project for testing and validating the commerce use case in @coveo/atomic",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "preview": "npx vite preview",
    "build": "npx vite build",
    "postinstall": "node ./scripts/copyResources.mjs"
  },
  "dependencies": {
    "@coveo/atomic": "^3.60.0",
    "@coveo/headless": "^3.53.0"
  },
  "devDependencies": {
    "fs-extra": "^11.3.6",
    "vite": "7.3.6"
  }
}